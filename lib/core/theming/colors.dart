import 'package:flutter/material.dart';

class ColorsManager {
  // Primary colors - Teal/Turquoise theme from design
  static const Color primary = Color(0xFF5DBEAA); // Teal/Turquoise
  static const Color primaryLight = Color(0xFF8FD4C4);
  static const Color primaryDark = Color(0xFF3A9C8A);

  // Basic colors
  static const Color white = Color(0xFFFFFFFF);
  static const Color black = Color(0xFF000000);
  static const Color grey = Color(0xFF9E9E9E);
  static const Color lightGrey = Color(0xFFF5F5F5);
  static const Color darkGrey = Color(0xFF616161);

  // Card/Surface colors
  static const Color cardBackground = Color(0xFFE8F5F3); // Light teal background
  static const Color searchBackground = Color(0xFFF5F5F5);

  // Status colors
  static const Color error = Color(0xFFB00020);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color info = Color(0xFF2196F3);
  static const Color location = Color(0xFFEF5350); // Red for location pin

  // Background colors
  static const Color background = Color(0xFFFAFAFA);
  static const Color surface = Color(0xFFFFFFFF);

  // Text colors
  static const Color textPrimary = Color(0xFF000000);
  static const Color textSecondary = Color(0xFF757575);
  static const Color textHint = Color(0xFFBDBDBD);
}
